services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - '127.0.0.1:5173:5173'
    networks:
      - medical_consultations_default

networks:
  medical_consultations_default:
    driver: bridge
